{
  "name": "google-mcp-server",
  "version": "0.1.0",
  "description": "FlatClaw per-user Google MCP — direct REST calls against Gmail / Drive / Calendar / Docs / Sheets / People APIs, capability-token-bridged to a fresh access token per call. No CLI dependency.",
  "main": "build/index.js",
  "type": "module",
  "bin": {
    "google-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsc --watch"
  },
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
